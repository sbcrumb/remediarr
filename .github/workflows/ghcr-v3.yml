name: Build & Push v3 to GHCR

on:
  pull_request:
    branches: [ v3 ]
  push:
    branches: [ v3 ]

permissions:
  contents: read
  packages: write

jobs:
  docker:
    runs-on: ubuntu-latest

    env:
      # PRs: single-arch build (faster). Push to v3: multi-arch.
      PLATFORMS: ${{ github.event_name == 'pull_request' && 'linux/amd64' || 'linux/amd64,linux/arm64' }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Compute v3 tags
        run: |
          SHA7="${GITHUB_SHA::7}"
          echo "SHA7=$SHA7" >> $GITHUB_ENV
          echo "V3_TAGS=ghcr.io/${GITHUB_REPOSITORY}:v3,ghcr.io/${GITHUB_REPOSITORY}:v3-${SHA7}" >> $GITHUB_ENV
          echo "Computed V3_TAGS=${V3_TAGS}"

      - name: Set up QEMU
        if: ${{ env.PLATFORMS == 'linux/amd64,linux/arm64' }}
        uses: docker/setup-qemu-action@v3

      - name: Set up Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        if: ${{ github.event_name != 'pull_request' }}
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build & (maybe) push
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          platforms: ${{ env.PLATFORMS }}
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ env.V3_TAGS }}
          build-args: |
            APP_VERSION=v3-${{ env.SHA7 }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          provenance: false
